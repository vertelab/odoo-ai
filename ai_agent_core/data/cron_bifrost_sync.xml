<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Cron: Sync models from Bifrost provider (weekly) -->
    <record id="cron_bifrost_sync" model="ir.cron">
        <field name="name">AI: Synca modeller från Bifrost</field>
        <field name="model_id" ref="model_ai_provider"/>
        <field name="state">code</field>
        <field name="code"># Sync models from all active bifrost providers
for provider in env['ai.provider'].search([('provider_type', '=', 'bifrost'), ('status', '=', 'confirmed')]):
    try:
        provider.action_fetch_models()
    except Exception:
        pass</field>
        <field name="interval_number">7</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
