<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Cron: Daily memory consolidation (runs at 02:00) -->
    <record id="cron_memory_consolidation" model="ir.cron">
        <field name="name">AI: Daglig minneskonsolidering</field>
        <field name="model_id" ref="model_ai_quest"/>
        <field name="state">code</field>
        <field name="code"># Consolidate memories for all active quests with identities
quests = env['ai.quest'].search([('status', '=', 'active'), ('identity_id', '!=', False)])
for quest in quests:
    try:
        quest.consolidate_memories()
    except Exception:
        pass</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
