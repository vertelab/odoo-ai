<?xml version="1.0" encoding="utf-8"?>

<odoo>

1) Internal user can only see and user server actions and use chats that are quests, on products cant see key
2) See own quests use_id on quest, can choose agents for that quest (change tools/memory/LLM????), cant see keys
3) See all quests and agents, choose tools/memory/llm for agent and llm for quest, can see all llm and venors but cant see keys, only systokens
4) Can see all keys and all types of tokens

    <data noupdate="1">

        <!-- <record id="contacts.res_partner_menu_config" model="ir.ui.menu">
            <field name="name">Configuration</field>
            <field name="groups_id" eval="[(4, ref('sales_team.group_sale_manager'))]"/>
        </record>
 -->
        <!-- <record id="module_ai_orchestration" model="ir.module.category"> 
<field name="name"></field>
<field name="parent_id"></field>
        </record> -->

       <record id="group_ai_agent_developer" model="res.groups">
            <field name="name">AI Agent Developer</field>
            <field name="category_id" ref="base.module_category_ai_orchestration"/>
            <field name="implied_ids" eval="[(6, 0, [ref('base.group_user')])]"/>
            <field name="comment">The user will be able to approve document created by employees.</field>
        </record>

        <record id="group_ai_agent_manager" model="res.groups">
            <field name="name">AI Agent Manager</field>
            <field name="comment">The user will have access to the human resources configuration as well as statistic reports.</field>
            <field name="category_id" ref="base.module_category_ai_orchestration"/>
            <field name="implied_ids" eval="[(6, 0, [ref('base.group_user')])]"/>
        </record>

        <record id="group_ai_agent_supervisor" model="res.groups">
            <field name="name">AI Agent Supervisor</field>
            <field name="comment">The user will have access to the human resources configuration as well as statistic reports.</field>
            <field name="category_id" ref="base.module_category_ai_orchestration"/>
            <field name="implied_ids" eval="[(6, 0, [ref('base.group_user'), ref('group_ai_agent_manager')])]"/>
            <field name="users" eval="[(4, ref('base.user_root')), (4, ref('base.user_admin'))]"/>
        </record>
        
    </data>

</odoo>
